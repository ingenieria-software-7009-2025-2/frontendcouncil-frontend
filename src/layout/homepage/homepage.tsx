import { JSX } from 'react';
import Mapa from '../map/map-page'

/**
 * Lay-out para página de inicio.
 * 
 * @returns {JSX.Element} Elemento correspondiente
 */
const HomePage = () => {
  return (
    <Mapa />
  );
};

/**
 * @module homepage
 * 
 * Lay-out de home-page.
 */
export default HomePage;